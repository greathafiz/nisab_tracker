echo "🔍 Running pre-commit checks on staged files..."

# Run lint-staged to lint and format only staged files
npx lint-staged || { echo "❌ Linting failed. Please fix the issues and try again."; exit 1; }

# Verify the build works
echo "🏗️ Verifying build..."
npm run build || { echo "❌ Build failed. Please fix the build issues and try again."; exit 1; }

echo "✅ Pre-commit checks passed! Committing changes..."
